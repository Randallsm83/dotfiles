# ~/.gitconfig — machine-specific git config
#
# This file exists so that `git config --global` writes land here
# instead of in ~/.config/git/config, which is managed by chezmoi
# and should not be modified at runtime.
#
# Main git configuration lives in ~/.config/git/config (chezmoi-managed).
# Settings here take precedence over the XDG config.
#
# Add machine-specific overrides below this line.
